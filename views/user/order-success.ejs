<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Success</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      font-family: 'Nunito Sans', sans-serif;
      background-color: #f9fdff;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 20px;
    }
    .success-card {
      background: white;
      padding: 40px 30px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    }
    .success-card i {
      font-size: 60px;
      color: #28a745;
      margin-bottom: 20px;
    }
    .success-card h1 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 15px;
      color: #2d3436;
    }
    .success-card p {
      color: #636e72;
      font-size: 1rem;
      margin-bottom: 25px;
    }
    .success-card .btn-custom {
      margin: 5px;
      padding: 12px 25px;
      border-radius: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .btn-primary-custom {
      background: linear-gradient(135deg, #ff3368, #ff6b9d);
      color: white;
      border: none;
    }
    .btn-outline-custom {
      border: 2px solid #ff3368;
      color: #ff3368;
      background: white;
    }
  </style>
</head>
<body>
  <div class="success-card">
    <i class="fas fa-check-circle"></i>
    <h1>Thank You!</h1>
    <p>Your order has been placed successfully.</p>
    <p><strong>Order ID:</strong> <%= order.orderId %></p>
    <div>
      <a href="/orders/<%= order._id %>" class="btn btn-primary-custom btn-custom">View Order</a>
      <a href="/shop" class="btn btn-outline-custom btn-custom">Continue Shopping</a>
    </div>
  </div>
</body>
</html>